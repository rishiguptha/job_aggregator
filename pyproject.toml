[project]
name = "job-aggregator"
version = "0.1.0"
description = "Multi-ATS Data Engineer Job Aggregator"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp==3.9.1",
    "python-dotenv>=1.0.1",
    "requests==2.31.0",
    "schedule==1.2.1",
]
